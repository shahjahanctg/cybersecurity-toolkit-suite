# data/yara — YARA rules directory (W6 malware tools)

Waves 5–8 (malware analysis, red-team) will place generated YARA rules and
classifier outputs here. The directory is configurable (Settings → YARA rule
directory). This folder ships empty; no sample malware is included in this
repository.